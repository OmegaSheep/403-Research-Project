#Makefile.
#Essentially it should perform: 
#g++ -std=c++11 -g skipList.C -o skipList

CC=g++

CFLAGS=-std=c++11 -g -Wall

skipList: skipList.C
	$(CC) $(CFLAGS) skipList.C -o skipList

clean:
	rm skipList
